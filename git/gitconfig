#
# ~/.gitconfig
#

[color]
	ui = auto
[alias]
	s = status
	c = commit
	a = add
	b = branch
	r = restore
	rs = restore --staged
	lg = log --graph
	lgs = log --graph --show-signature
	gc = git gc --aggressive --prune=now
	untrack = update-index --assume-unchanged
[user]
	email = jolinzhou90@gmail.com
	name = jiulingz
[core]
	excludesfile = ~/.gitignore
[commit]
	gpgsign = true
[pull]
	ff = only
[init]
	defaultBranch = main
